<UserControl x:Class="DoctorApplication.MVVM.View.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:m="clr-namespace:DoctorApplication.MVVM.ViewModel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoctorApplication.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{d:DesignInstance Type={x:Type m:HistoryViewModel}}"
             >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Style="{StaticResource HeaderStyle1}" FontSize="15" Content="Session History" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Global User Interface"/>
        <Label Grid.Row="0" Grid.Column="1" Style="{StaticResource HeaderStyle1}" FontSize="15" Content="Bike Data History" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Global User Interface"/>
        <Label Grid.Row="0" Grid.Column="2" Style="{StaticResource HeaderStyle1}" FontSize="15" Content="Heart Data History" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Global User Interface"/>
        <StackPanel Grid.Row="1">
            <Label HorizontalAlignment="Center" Style="{StaticResource LabelStyleMain}" Content="Select User"/>

            <ComboBox
            Height="30"
            ItemsSource="{Binding users}"
            SelectedItem="{Binding SelectedUser}"
            Width="150">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding UserName}" />
                            <TextBlock Padding="10,0" Text="{Binding BikeId}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Label HorizontalAlignment="Center"  Style="{StaticResource LabelStyleMain}" Content="Select Session"/>
            
            <ComboBox
            Height="30"
            ItemsSource="{Binding sessions}"
            SelectedItem="{Binding SelectedSession}"
            Width="150">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding UserName}" />
                            <TextBlock Padding="10,0" Text="{Binding BikeId}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
